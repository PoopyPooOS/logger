[package]
name = "logger"
version = "0.1.0"
edition.workspace = true

[features]
log = ["dep:log"]

[dependencies]
colored = "2.1.0"
log = { version = "0.4.22", optional = true }

[lints]
workspace = true
